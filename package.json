{
  "name": "markdown-previewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "marked": "^0.5.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.5",
    "deploy": "push-dir --dir=build --branch=gh-pages --cleanup"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  },
  "homepage": "http://VERSAYANA.github.io/markdown-previewer"
}
